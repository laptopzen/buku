extends layout/default.pug

block isi
  .list-group
    each x in post
      a.list-group-item(href=`post/${x.judul.replace(/ /g, '-')}.html`)
        strong #{x.judul}
        br
        em #{x.penulis}